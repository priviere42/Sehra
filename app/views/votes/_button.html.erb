
<% if user_signed_in? %>

<% if current_user.vote?(@event) %>
  <%= button_to "ANNULER", unvote_path(event_id: @event.id), remote: true, method: :delete, class:"btn btn-primary"%>
<% else %>
  <%= button_to "VOTER", vote_path(event_id: @event.id), remote: true, class:"btn btn-primary"%>
<% end %>



<% else %> 

Vous devez vous connecter pour voter pour ce projet.


<% end %>
<label><%= @event.votes.count %> VOTES !
    <div class="progress progress-xxs center" style="width:60em; max-width: 60em; height: 3.5em;">
   		<div class="progress-bar" style="width: <%= (((@event.votes.count)*100)/User.count)*0.6 %>em; max-width: 60em; height: 3.5em; background-color: #99D17B"></div>
    </div>

    <div class="percentage"><b> <%= ((@event.votes.count)*100)/User.count %> %</b> <br> des membres ont été convaincus par ce projet.  </div>
    
</div>
</label>